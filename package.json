{
  "name": "hidden-latent-space",
  "version": "1.0.0",
  "description": "A dynamic, retro-terminal text adventure game powered by Gemini.",
  "private": true,
  "scripts": {
    "start": "serve -s .",
    "build": "esbuild index.tsx --bundle --outfile=bundle.js --format=esm --jsx=automatic --define:process.env.API_KEY='\"$API_KEY\"' --external:react --external:react/* --external:react-dom/* --external:@google/genai"
  },
  "dependencies": {
    "serve": "^14.2.3",
    "esbuild": "^0.20.2"
  }
}