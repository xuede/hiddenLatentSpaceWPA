{
  "name": "gemini-text-adventure",
  "version": "1.0.0",
  "description": "A dynamic, retro-terminal text adventure game powered by Gemini.",
  "private": true,
  "scripts": {
    "start": "serve -s .",
    "build": "esbuild index.tsx --bundle --outfile=bundle.js --format=esm --jsx=automatic"
  },
  "dependencies": {
    "serve": "^14.2.3",
    "esbuild": "^0.20.2"
  }
}